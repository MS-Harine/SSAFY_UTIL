<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SSAFY_UTIL.View.Component.SettingItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SSAFY_UTIL.View.Component"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300">

    <Grid Margin="0,0,0,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row="0" 
            Text="{x:Bind Title}" 
            Style="{StaticResource MarginedSubtitleTextBlockStyle}"/>
        <TextBlock 
            Grid.Row="1" 
            Text="{x:Bind Caption}" 
            Style="{StaticResource MarginedBodyTextBlockStyle}"/>
        <ContentPresenter 
            Grid.Row="2" 
            Content="{x:Bind ActionContent}" />
    </Grid>
</UserControl>
